/**
 * Temporary regulatory rollout flags.
 * Keep these centralized so UI + API behavior stays consistent.
 */
export const CREATOR_LOGIN_ENABLED = false;
export const CREATOR_ONBOARDING_ENABLED = false;
export const PHONE_MFA_ENABLED = false;

